import { TippingApp } from "@/components/tipping/TippingApp";

export const metadata = {
  title: "five tipping point — less chaos, same life",
  description:
    "Set a boundary you actually live with. Notice the days you stayed inside it. Not abstinence — minimal negative.",
};

export default function TippingPointPage() {
  return <TippingApp />;
}
