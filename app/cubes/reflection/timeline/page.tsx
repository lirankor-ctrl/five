import { TimelineView } from "@/components/reflection/TimelineView";

export const metadata = {
  title: "river timeline — five reflection",
  description:
    "A 26-week view of where presence has lived in your life across the cubes. Not a leaderboard. A long-arc map.",
};

export default function TimelinePage() {
  return <TimelineView />;
}
