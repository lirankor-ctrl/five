import { ReportView } from "@/components/reflection/ReportView";

export const metadata = {
  title: "this week — five reflection",
  description:
    "A calm narrative document about how you have been moving across the cubes this week.",
};

export default function ReflectionReportPage() {
  return <ReportView />;
}
