import { ArchiveView } from "@/components/reflection/ArchiveView";

export const metadata = {
  title: "archive — five reflection",
  description: "Past reflection reports you have saved.",
};

export default function ArchivePage() {
  return <ArchiveView />;
}
