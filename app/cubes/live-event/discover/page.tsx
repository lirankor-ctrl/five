import { DiscoverView } from "@/components/live/DiscoverView";

export const metadata = {
  title: "discover — five live event",
  description:
    "Quiet shared lives, sorted by what is alive right now. Live now, starting soon, today, this week.",
};

export default function DiscoverPage() {
  return <DiscoverView />;
}
