import { SoloApp } from "@/components/solo/SoloApp";

export const metadata = {
  title: "five solo — a quiet place to return to",
  description:
    "Choose one small real-life action. Return to it. Report briefly. Nothing more.",
};

export default function SoloPage() {
  return <SoloApp />;
}
