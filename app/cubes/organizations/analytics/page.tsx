import { AnalyticsView } from "@/components/organizations/AnalyticsView";

export const metadata = {
  title: "analytics — five organizations",
  description:
    "A calm executive view of organizational momentum. Density of human moments — not surveillance.",
};

export default function AnalyticsPage() {
  return <AnalyticsView />;
}
