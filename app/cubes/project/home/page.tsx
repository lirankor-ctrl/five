import { ProjectHome } from "@/components/project/ProjectHome";

export const metadata = {
  title: "your projects — five project",
  description:
    "Your studio. Begin a new project four ways: quick, from a template, from a hazy dream, or rescue an abandoned one.",
};

export default function ProjectHomePage() {
  return <ProjectHome />;
}
