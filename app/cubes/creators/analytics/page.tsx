import { AnalyticsView } from "@/components/creators/AnalyticsView";

export const metadata = {
  title: "analytics — five creators",
  description:
    "A calm creator analytics dashboard: human-impact marks, retention, return behaviour, and a future revenue layer.",
};

export default function AnalyticsPage() {
  return <AnalyticsView />;
}
