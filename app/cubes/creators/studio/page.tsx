import { StudioView } from "@/components/creators/StudioView";

export const metadata = {
  title: "studio — five creators",
  description:
    "A calm content builder for small five-minute sessions. Type, attributes, media composition, structure — saved locally today, CMS-ready tomorrow.",
};

export default function StudioPage() {
  return <StudioView />;
}
