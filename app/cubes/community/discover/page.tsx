import { DiscoverView } from "@/components/community/DiscoverView";

export const metadata = {
  title: "discover — five community",
  description:
    "A calm map of small communities sorted by what fits you — interest, identity, or quietly several at once.",
};

export default function DiscoverPage() {
  return <DiscoverView />;
}
