import { ContentApp } from "@/components/content/ContentApp";

export const metadata = {
  title: "five content — a focused internet in five-minute portions",
  description:
    "Turn the internet into a five-minute growth engine. Type what you want, and find something existing, real, and small enough to finish.",
};

export default function ContentPage() {
  return <ContentApp />;
}
